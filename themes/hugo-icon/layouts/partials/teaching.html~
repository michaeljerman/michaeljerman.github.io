<div id="fh5co-services" data-section="teaching">
  {{ with .Site.Params.teaching }}
  <div class="container">
    <div class="col-md-6 col-md-offset-3 text-center fh5co-heading">
      <h2>{{ .title }}</h2>
      <p>{{ .description | markdownify }}</p>
    </div>
    
    {{ range .item }}
    <div class="col-md-4">
      <div class="service">
	<h3>{{ .name | markdownify}}</h3>
	<p class="terms">{{ .terms | markdownify }}</p>
	<p class="links">
	  <a href= {{ .syllabus }} class="btn btn-primary btn-outline" target="_blank"> syllabus </a>
	  {{ if .website }}
	  <a href={{ .website }} class="btn btn-primary btn-outline popup-vimeo"> Lecture notes </a> </p>
	{{end}}
      </div>
    </div>
    {{ end }}
    
    
  </div>
  {{ end }}
</div> <!-- END fh5co-teaching -->
